const SearchBar = () => {

    return (
        <div className="search-bar">
            <input 
                placeholder="Search location..."
            />
            <button>Search</button>
        </div>
    )
}

export default SearchBar;